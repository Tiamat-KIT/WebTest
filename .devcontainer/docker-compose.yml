version: '3'
services:
  playground:
    container_name: 'node'
    hostname: 'node'
    build: .
    restart: always
    working_dir: '/opt'
    tty: true
    volumes:
      - type: bind
        source: ../opt
        target: /opt
